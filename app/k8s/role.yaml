apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: linkguard-pod-reader
  namespace: linkguard
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - watch